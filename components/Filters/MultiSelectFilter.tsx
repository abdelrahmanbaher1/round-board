import React from "react";

type TProps = {
  options: string[];
  filterKey: string;
  title: string;
};
const MultiSelectFilter = ({ options, title }: TProps) => {
  return;
};

export default MultiSelectFilter;
