import React from "react";

const Filters = ({
  filterType,
  fiterTitle,
}: {
  filterType: "multiSelect" | "toggle";
  fiterTitle?: string;
}) => {
  
    switch(filterType){
        case 'multiSelect':
            <Mul
    }
  
};

export default Filters;
